// escreva uma funcao que recebe dois numeros e retorne o maior deles 

const vs = (x, y) => x > y ? x : y;
console.log(vs(10,20));

// escreva uma funcao chamada ePaisagem que recebe dois argumentos, largura, altura de uma imagem (Number)
// retorne true se a imagem estiver no modo paisagem 

// eixo X para largura, eixo Y para altura 

const ePaisagem = (largura, altura) => largura > altura;
console.log(ePaisagem(100,300));

// Escreva uma funcao que recebe um numero e retorne o seguinte: 
// Numero divisivel por 3 = Fizz 
// Numero divisivel por 5 = Buzz 
// Numero divisivel por 3 e 5 = FizzBuzz 
// Numero nao divisivel por 3 e 5 = Retorna o propio numero 
// Checar se o numero e realmente um numero
// Use a funcao com numeros de 0 a 100

const oni = (n) => n / 3;

console.log(n(1));